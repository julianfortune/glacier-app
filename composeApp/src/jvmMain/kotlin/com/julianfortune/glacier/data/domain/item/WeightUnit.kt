package com.julianfortune.glacier.data.domain.item

// TODO(P2): Update to get away from 'weight'--needs to represent the quantity more generally (e.g., could be bunches of bananas, heads of lettuce, etc.)
enum class WeightUnit {
    OZ,
    LB
}
